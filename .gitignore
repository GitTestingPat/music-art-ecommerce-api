# Environment variables - Critical
.env
.env.local
.env.*.local
.env.production

# Logs - Don't commit log files
logs/
*.log
npm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Config files that may contain sensitive information
Thumbs.db
.DS_Store
.DS_Store?
._*
.Trashes
.Spotlight-V100
ehthumbs.db
Thumbs.db

# Config files compose
docker-compose.override.yml
docker-compose.*.yml

# Config files of Kubernetes that may contain environment variables
k8s/*.yaml
k8s/*.yml

# Config files of CI/CD that may contain environment variables
.ci/*.yml
.ci/*.yaml
.cicd/*.yml
.cicd/*.yaml

# Config files of cloud services that may contain environment variables
.cloud/*.yml
.cloud/*.yaml
.cloud/*.json
.cloud/*.env

# Config files of development tools that may contain environment variables
.vscode/settings.json
.idea/workspace.xml
.idea/*.iml

# Config files of tests that may contain environment variables
tests/.env
tests/.env.local
tests/.env.*.local

# Config files of scripts that may contain environment variables
scripts/.env
scripts/.env.local
scripts/.env.*.local  

# Config files of documentation that may contain environment variables
package.json.env
yarn.lock.env
pnpm-lock.yaml.env

# Config files of build tools that may contain environment variables
webpack.config.env.js
rollup.config.env.js
gulpfile.env.js
gruntfile.env.js

# Config files of external services that may contain environment variables
aws-exports.env.js
firebase-config.env.js

# Config files of containers that may contain environment variables
Dockerfile.env
docker-compose.env.yml

# Config files of orchestrators that may contain environment variables
kubernetes.env.yaml
helm-values.env.yaml

# Config files of monitoring that may contain environment variables
prometheus.env.yml
grafana.env.ini

# Config files of security that may contain environment variables
vault.env.yml

# Config files of static analysis that may contain environment variables
sentry.env.yml

# Config files of documentation that may contain environment variables
docs/.env   
docs/.env.local
docs/.env.*.local

# Config files of localization that may contain environment variables
locales/.env
locales/.env.local
locales/.env.*.local

# Config files of migrations that may contain environment variables
migrations/.env
migrations/.env.local
migrations/.env.*.local

# Config files of seeds that may contain environment variables
seeds/.env
seeds/.env.local
seeds/.env.*.local

# Config files of documentation that may contain environment variables
docs/.env
docs/.env.local
docs/.env.*.local

# Config files of localization that may contain environment variables
locales/.env
locales/.env.local
locales/.env.*.local

# Config files of migrations that may contain environment variables
migrations/.env
migrations/.env.local
migrations/.env.*.local

# Config files of seeds that may contain environment variables
seeds/.env
seeds/.env.local
seeds/.env.*.local  

# Config files of backups that may contain environment variables
backups/.env
backups/.env.local
backups/.env.*.local 

# Config files of integrations that may contain environment variables
integrations/.env
integrations/.env.local
integrations/.env.*.local

# Config files of deployment that may contain environment variables
deploy/.env
deploy/.env.local
deploy/.env.*.local

# Config files of static analysis that may contain environment variables
static-analysis/.env
static-analysis/.env.local
static-analysis/.env.*.local

# Config files of code generation that may contain environment variables
codegen/.env
codegen/.env.local
codegen/.env.*.local

# User uploaded files
uploads

# IDE files
.idea/
.vscode/
*.swp
*.swo
*~
node_modules/
package-lock.json

# Build directories
dist/
build/

# Testing
.cache/
coverage/
.nyc_output/

# Temporal files
tmp/
temp/
*.tmp
EOF

# Local Data Bases (SQlite)
*.db
*.sqlite
*.sqlite3